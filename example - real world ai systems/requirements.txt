# Core demos (1-6, 9, 11)
openai>=3.17
numpy
langchain>=1.4.2
langchain-openai>=1.6.3
langgraph>=1.2.12

# MCP demos (7, 8)
# The client side is `langchain.mcp`, which ships inside langchain itself and
# is built on fastmcp. Do NOT add `langchain-mcp-adapters`: that package was
# superseded in 2026, still requires mcp<2, and installing it drags the whole
# tree back to an older SDK.
fastmcp>=4

# Speech demos (12, 13)
# Demo 10 is a shell script for a GPU server and needs `pip install vllm` there.
piper-tts
faster-whisper
